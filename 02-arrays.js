// Arrays are useful for representing ordered lists.
let genes = ['BRCA1', 'TP53', 'PCSK9'];

// Look at the array
console.log(genes);

// Get the first element
console.log(genes[0]);

// Get the number of elements
console.log(genes.length);
