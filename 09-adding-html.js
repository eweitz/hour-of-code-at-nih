// Here we add a labeled text input to the HTML page, using jQuery -- as
// indicated by the "$" character
$('body').append(
  '<label for="geneInput">Gene: </label>' +
  '<input id="geneInput" type="text" />'
);
